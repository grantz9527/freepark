spring:
  datasource:
    url: jdbc:h2:mem:freepark;MODE=MySQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: create-drop

freepark:
  jwt:
    secret: freepark-cloud-test-secret-change-me-32
    ttl: 1h
  admin:
    username: admin
    password: admin123
    display-name: Administrator
