Linux and macOS shell scripts. Run from the git repository root:

```sh
sh ai-build/linux/up.sh
sh ai-build/linux/install.sh
# start local_server then local_frontend (see ai-build/AGENTS.md)
sh ai-build/linux/verify.sh
```

Needs Docker Engine (or Docker Desktop on Mac), JDK 21, Node.js 22+.
