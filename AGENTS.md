# FreePark agent notes

To **run or deploy** this repo on a machine, follow [`ai-build/AGENTS.md`](ai-build/AGENTS.md).

Do not start from scattered READMEs. The deploy contract (ports, order, health checks, what is optional) lives only in `ai-build/`.
